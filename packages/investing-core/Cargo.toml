[package]
name = "investing-core"
version = "0.1.0"
edition = "2021"
authors = ["Adolago"]
license = "MIT"
description = "Core trading and portfolio management library for the investing domain"
repository = "https://github.com/adolago/zee"

[lib]
name = "investing_core"
path = "src/lib.rs"

[[bin]]
name = "investing"
path = "src/bin/investing.rs"
required-features = ["cli"]

[dependencies]
# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Time handling
chrono = { version = "0.4", features = ["serde"] }

# Error handling
thiserror = "2.0"

# CLI (optional)
clap = { version = "4.0", features = ["derive"], optional = true }

# Directories for default paths
directories = "6.0"

[features]
default = []
cli = ["clap"]

[dev-dependencies]
tempfile = "3.14"
approx = "0.5"
