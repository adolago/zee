// Defaults for agent metadata when upstream does not supply them.
// Default model for Zee when no explicit config/default is available.
export const DEFAULT_PROVIDER = "cerebras";
export const DEFAULT_MODEL = "zai-glm-4.7";
// Default context window for Zee when no model limit is provided.
export const DEFAULT_CONTEXT_TOKENS = 262_144;
