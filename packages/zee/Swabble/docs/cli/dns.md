---
summary: "CLI reference for `zee dns` (wide-area discovery helpers)"
read_when:
  - You want wide-area discovery (DNS-SD) via Tailscale + CoreDNS
  - You’re setting up split DNS for zee.internal
---

# `zee dns`

DNS helpers for wide-area discovery (Tailscale + CoreDNS). Currently focused on Homebrew CoreDNS installs.

Related:
- Gateway discovery: [Discovery](/gateway/discovery)
- Wide-area discovery config: [Configuration](/gateway/configuration)

## Setup

```bash
zee dns setup
zee dns setup --apply
```
