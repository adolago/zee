services:
  zee-gateway:
    image: ${ZEE_IMAGE:-zee:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      ZEE_GATEWAY_TOKEN: ${ZEE_GATEWAY_TOKEN}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE}
    volumes:
      - ${ZEE_CONFIG_DIR}:/home/node/.zee
      - ${ZEE_WORKSPACE_DIR}:/home/node/zee
    ports:
      - "${ZEE_GATEWAY_PORT:-18789}:18789"
      - "${ZEE_BRIDGE_PORT:-18790}:18790"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${ZEE_GATEWAY_BIND:-lan}",
        "--port",
        "${ZEE_GATEWAY_PORT:-18789}"
      ]

  zee-cli:
    image: ${ZEE_IMAGE:-zee:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      BROWSER: echo
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE}
    volumes:
      - ${ZEE_CONFIG_DIR}:/home/node/.zee
      - ${ZEE_WORKSPACE_DIR}:/home/node/zee
    stdin_open: true
    tty: true
    init: true
    entrypoint: ["node", "dist/index.js"]
