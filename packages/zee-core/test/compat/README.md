# Parity Test Anchors

This folder contains placeholders for parity test fixtures and golden outputs.
The anchors map to the Phase 0 parity docs.

Subdirectories:
- `ansible/`
- `terraform/`
- `cli/`
