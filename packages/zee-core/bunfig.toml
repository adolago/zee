preload = ["@opentui/solid/preload"]

[test]
preload = ["./test/preload.ts"]
timeout = 10000  # 10 seconds (default is 5000ms)
# Enable code coverage
coverage = true
# Only search for tests in the test directory, not dist
root = "./test"
