# Stanley Core Requirements
# Focus on institutional data sources and money flow analysis

# Core dependencies
fastapi>=0.115.0  # Brings starlette>=0.47.2 with security fixes
uvicorn>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
PyJWT>=2.10.1  # CVE-2024-53861 fix
passlib[bcrypt]>=1.7.4
email-validator>=2.0.0
python-multipart>=0.0.18  # CVE-2024-53981 fix (starlette dependency)

# Data handling
pandas>=2.1.0
numpy>=1.25.0
polars>=0.19.0
sqlalchemy>=2.0.20
psycopg2-binary>=2.9.9
redis>=5.0.0

# Financial data
openbb>=4.6.0  # Updated for fastapi/python-multipart compatibility
yfinance>=0.2.24
sec-downloader>=0.12.0
edgartools>=3.0.0
dbnomics>=1.2.0
requests>=2.32.4
aiohttp>=3.13.3  # Multiple CVE fixes (CVE-2025-69223 through CVE-2025-69230)

# Algorithmic trading
nautilus_trader>=1.200.0

# Analytics
scipy>=1.11.0
statsmodels>=0.14.0

# NOTE: empyrical/pyfolio are currently incompatible with Python 3.13
# (empyrical uses deprecated configparser.SafeConfigParser). If we need these
# metrics later, prefer implementing the small set of required stats directly
# with numpy/pandas or adopting a maintained alternative.

# Vector search (Qdrant)
qdrant-client>=1.7.0
fastembed>=0.2.0

# Optional: Additional data sources
# quandl==3.7.0
# intrinio-sdk==6.24.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Development
black>=23.11.0
flake8>=6.1.0
mypy>=1.7.0
pre-commit>=3.6.0
urllib3>=2.6.3 # not directly required, pinned by Snyk to avoid a vulnerability
protobuf>=6.31.1 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
dnspython>=2.6.1 # not directly required, pinned by Snyk to avoid a vulnerability
xmltodict>=0.13.0,<0.14.0 # pinned for compatibility with openbb-sec (which requires <0.14.0)
